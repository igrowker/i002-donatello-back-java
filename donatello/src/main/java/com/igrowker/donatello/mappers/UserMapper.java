package com.igrowker.donatello.mappers;

import org.springframework.stereotype.Component;

@Component
public class UserMapper {
/*
    public UserReadDto toReadDto(CustomUser u){
        return UserReadDto.builder()
                .id(u.getId())
                .username(u.getUsername())
                .dni(u.getDni())
                .phone(u.getPhone())
                .emergencyPhone(u.getEmergencyPhone())
                .firstName(u.getFirstName())
                .lastName(u.getLastName())
                .role(u.getRole())
                //.profileImage(u.getUserProfileImage())
                .email(u.getEmail())
                .build();
    }


    public UserStaffReadDto toStaffReadDto(CustomUser u){
        return UserStaffReadDto.builder()
                .id(u.getId())
                .username(u.getUsername())
                .email(u.getEmail())
                .dni(u.getDni())
                .phone(u.getPhone())
                .emergencyPhone(u.getEmergencyPhone())
                .firstName(u.getFirstName())
                .lastName(u.getLastName())
                .role(u.getRole())
                //.profileImage(u.getUserProfileImage())
                .isAccountNonExpired(u.isAccountNonExpired())
                .isAccountNonLocked(u.isAccountNonLocked())
                .isCredentialsNonExpired(u.isCredentialsNonExpired())
                .isEnabled(u.isEnabled())
                .authorities(u.getAuthorities())
                .build();
    }

 */
}
